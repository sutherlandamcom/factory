---
import type { SiteProfileNavigationEntry } from "@factory/contracts";

interface Props {
  siteName: string;
  navigation: SiteProfileNavigationEntry[];
  addressLines: string[];
}

const { siteName, navigation, addressLines } = Astro.props;
const year = new Date().getFullYear();
---

<footer class="border-t border-slate-line bg-paper">
  <div
    class="mx-auto flex max-w-6xl flex-wrap items-center justify-between gap-4 px-6 py-8 text-sm text-ink-500"
  >
    <div>
      <p>&copy; {year} {siteName}. All rights reserved.</p>
      {
        addressLines.length > 0 && (
          <p class="mt-1">
            {addressLines.map((line, index) => (
              <>
                {index > 0 && " · "}
                {line}
              </>
            ))}
          </p>
        )
      }
    </div>
    <nav aria-label="Footer navigation">
      <ul class="flex gap-6">
        {
          navigation.map((item) => (
            <li>
              <a href={item.targetSlug} class="transition-colors hover:text-accent">
                {item.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</footer>
